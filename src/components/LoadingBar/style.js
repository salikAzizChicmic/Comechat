import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: { flex: 1, height: '100%', width: "100%", justifyContent: 'center', alignItems: 'center' },
    box: { height: '16%', width: '50%', backgroundColor: "lightgrey", marginTop: 20 },
    txtDesign:{textAlign:'center',fontSize:20}
});
  
export default styles;